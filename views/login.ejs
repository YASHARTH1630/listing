<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Signup – Travel Listings</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <style>
        * {
            box-sizing: border-box;
            font-family: "Segoe UI", Tahoma, sans-serif;
        }
        
        body {
            min-height: 100vh;
            margin: 0;
            background: linear-gradient(135deg, #8dbbce, #a6b7bd, #607c88);
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 1rem;
        }
        
        .main-heading {
            text-align: center;
            font-size: 1.8rem;
            font-weight: 800;
            letter-spacing: 2px;
            color: #2c5364;
            margin-bottom: 0.3rem;
            margin-right: 1.2rem;
        }
        
        .sub-heading {
            text-align: center;
            font-size: 0.9rem;
            color: #666;
            margin-bottom: 1.8rem;
            margin-left: 1.1rem;
        }
        /* Responsive */
        
        @media (max-width: 480px) {
            .main-heading {
                font-size: 1.5rem;
                letter-spacing: 1.5px;
            }
        }
        
        .signup-card {
            background: #ffffff;
            width: 100%;
            max-width: 420px;
            padding: 2rem;
            border-radius: 14px;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.2);
            animation: fadeIn 0.6s ease-in-out;
        }
        
        .signup-card h2 {
            text-align: center;
            margin-bottom: 1.5rem;
            color: #2c5364;
        }
        
        label {
            font-size: 0.9rem;
            color: #555;
            margin-bottom: 0.3rem;
            display: block;
        }
        
        input {
            width: 100%;
            padding: 0.75rem;
            margin-bottom: 1.2rem;
            border-radius: 8px;
            border: 1px solid #ccc;
            font-size: 0.95rem;
            transition: border 0.3s, box-shadow 0.3s;
        }
        
        input:focus {
            outline: none;
            border-color: #2c5364;
            box-shadow: 0 0 0 2px rgba(44, 83, 100, 0.2);
        }
        
        button {
            width: 100%;
            padding: 0.8rem;
            background: #2c5364;
            color: #fff;
            font-size: 1rem;
            font-weight: 600;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            transition: background 0.3s, transform 0.2s;
        }
        
        button:hover {
            background: #1b3b48;
            transform: translateY(-2px);
        }
        
        .footer-text {
            text-align: center;
            margin-top: 1rem;
            font-size: 0.85rem;
            color: #777;
        }
        
        .footer-text a {
            color: #2c5364;
            text-decoration: none;
            font-weight: 600;
        }
        
        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(10px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        /* Responsive tweaks */
        
        @media (max-width: 480px) {
            .signup-card {
                padding: 1.5rem;
            }
            button {
                font-size: 0.95rem;
            }
        }
    </style>
</head>

<body>

    <div class="head">
        <h2 class="main-heading">TRAVEL LISTING</h2>

    </div>


    <div class="signup-card">
        <h2>LOGIN HERE</h2>
        <p class="sub-heading">Let's together explore destinations
            <h4 style="color:red; text-align:center;">
                <%= error %>
            </h4>
        </p>
        <form action="/signup/login/post" method="POST">

            <label for="username">Username</label>
            <input type="text" id="username" name="username" required>

            <label for="password">Password</label>
            <input type="password" id="password" name="password" required>

            <button type="submit">Login</button>
        </form>

        <div class="footer-text">
            CREATE NEW
            <a href="/signup">Signup</a>
        </div>
    </div>

</body>

</html>
</div>

</body>

</html>